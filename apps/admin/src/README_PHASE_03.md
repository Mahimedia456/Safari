# Safari Admin — Phase 03

Added real API integration for passenger management:
- passenger list
- search/status/country filters
- passenger detail endpoint
- status update (active/suspended/blocked)
- real profile, saved address, preference and emergency-contact data
- Zustand passenger store

The existing admin UI/design can consume `usePassengerStore`.
No mock passenger dataset is required for Phase 03 routes.

Demo admin:
admin@safari.com
password: safarimobile
